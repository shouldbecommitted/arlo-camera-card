// `__CARD_VERSION__` is replaced at build time with the package.json version
// (see rollup.config.js). The literal here is the fallback for unbuilt/dev runs.
export const CARD_VERSION = "__CARD_VERSION__";
